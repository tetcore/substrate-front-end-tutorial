"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.approvalFlagToBool = approvalFlagToBool;

var _bn = _interopRequireDefault(require("bn.js"));

// Copyright 2017-2019 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
function approvalFlagToBool(flags) {
  const bools = [];

  for (const flag of flags) {
    for (const bit of [...Array(flag.bitLength())].map((_, i) => i)) {
      bools.push(!flag.toBn().uand(new _bn.default(1).shln(bit)).isZero());
    }
  }

  const lastApproval = bools.lastIndexOf(true);
  return lastApproval >= 0 ? bools.slice(0, lastApproval + 1) : [];
}