import { CodecTypes } from '@polkadot/types/classes';
import { RpcParam } from '../types';
interface RpcParamOptions {
    isOptional?: boolean;
}
export default function createParam(name: string, type: CodecTypes, { isOptional }?: RpcParamOptions): RpcParam;
export {};
