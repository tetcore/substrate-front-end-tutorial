import { Codec } from '../../types';
/**
 * Given the static `rpcData` and the `latestSubstrate` JSON file, Metadata
 * should decode `rpcData` and output `latestSubstrate`.
 */
export declare function decodeLatestSubstrate<Modules extends Codec>(version: number, rpcData: string, latestSubstrate: object): void;
/**
 * Given a `version`, MetadataV7 and MetadataV{version} should output the same
 * unique types.
 */
export declare function toV7<Modules extends Codec>(version: number, rpcData: string): void;
/**
 * Given a Metadata, no type should throw when given its fallback value.
 */
export declare function defaultValues(rpcData: string): void;
