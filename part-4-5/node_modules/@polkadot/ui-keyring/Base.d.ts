import { KeyringInstance, KeyringPair } from '@polkadot/keyring/types';
import { AddressSubject } from './observable/types';
import { KeyringOptions, KeyringStore } from './types';
export default class Base {
    private _accounts;
    private _addresses;
    private _contracts;
    private _keyring?;
    private _prefix?;
    protected _genesisHash?: string;
    protected _store: KeyringStore;
    constructor();
    readonly accounts: AddressSubject;
    readonly addresses: AddressSubject;
    readonly contracts: AddressSubject;
    readonly keyring: KeyringInstance;
    readonly genesisHash: string | undefined;
    decodeAddress: (key: string | Uint8Array, ignoreChecksum?: boolean | undefined) => Uint8Array;
    encodeAddress: (key: string | Uint8Array) => string;
    getPair(address: string | Uint8Array): KeyringPair;
    getPairs(): KeyringPair[];
    isAvailable(_address: Uint8Array | string): boolean;
    isPassValid(password: string): boolean;
    setAddressPrefix(prefix: number): void;
    setDevMode(isDevelopment: boolean): void;
    protected initKeyring(options: KeyringOptions): void;
    protected addAccountPairs(): void;
    protected addTimestamp(pair: KeyringPair): void;
}
